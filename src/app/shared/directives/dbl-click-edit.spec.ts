import { DblClickEdit } from './dbl-click-edit';

describe('DblClickEdit', () => {
  it('should create an instance', () => {
    const directive = new DblClickEdit();
    expect(directive).toBeTruthy();
  });
});
